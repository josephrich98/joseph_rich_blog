# Header navigation. Order here = order in the masthead.
# (The site title itself links to the About / home page.)
main:
  - title: "Blog"
    url: /blog/

  - title: "Publications"
    url: /publications/

  - title: "Software"
    url: /software/

  # Draft section: its page and its _data/models_local.yml content are
  # gitignored, so this link only renders where that data file exists (i.e.
  # locally). Drop requires_data when the section goes public.
  - title: "Models"
    url: /models/
    requires_data: models_local

  - title: "Videos"
    url: /videos/

  # Draft section: its page and its _data/funding_local.yml content are
  # gitignored, so this link only renders where that data file exists (i.e.
  # locally). Drop requires_data when the section goes public.
  - title: "Funding"
    url: /funding/
    requires_data: funding_local

  - title: "Contact"
    url: /contact/
